# Next Steps Plan - D1-Only Content Index Tranche

## Answer to Current Question

Yes. There is an already planned next task sequence, and one item is still open from the authoritative handoff: remote Cloudflare parity validation after the migration tranche.

Primary references:

- `plans/adrs/0016-d1-as-canonical-cloud-content-index-and-r2-blob-storage.md`
- `plans/d1-manifest-removal-and-d1-index-hardening-handoff-to-code-2026-04-05.md`

## Current State

- Manifest removal + D1 hardening code tranche has been implemented.
- Local validation passed (`pnpm test`, `pnpm build`, local migration checks).
- Production remote migration plan now completes successfully.
- Production content sync now completes successfully.
- Remote D1 schema now verifies `PRIMARY KEY (collection, id)` and all current rows have populated `r2_key` values.
- Remote parity build now completes successfully with `CONTENT_SOURCE_MODE=cloud CONTENT_LOADER_D1_MODE=remote`.

## Planned Next Tasks (Ordered)

1. **Apply remote migration plan cleanly in prod**
   - Completed: `pnpm db:migrate:plan:prod` now succeeds.
   - Result: `0008_content_index_collection_scoped_identity.sql` is effectively applied in remote D1.

2. **Verify remote `content_index` constraints match expected schema**
   - Completed: composite identity exists as `(collection, id)` and supports `ON CONFLICT(collection, id)`.
   - Completed: `r2_key` exists and is populated for all current rows after sync repair.

3. **Re-run production content sync**
   - Completed: `pnpm content:sync:prod` succeeds.
   - Result: no `SYNC-CONTENT-INDEX-FAILED` from conflict mismatch.

4. **Validate remote parity lane (authoritative follow-up)**
   - Completed: authenticated remote build succeeded.
   - Result: end-to-end D1 lookup + R2 body fetch behavior is validated in remote mode.

5. **Operational close-out**
   - In progress: capture final pass evidence in authoritative docs.
   - Remaining optional step: prepare a commit for migration + sync writer safeguards + doc updates.

## Immediate Risk Checks

- Ensure the migration runner does not partially apply index/table changes if interrupted.
- Confirm no residual remote table shape still keyed by global `id`.
- Confirm sync upsert safeguard retains prior `r2_key` when an incoming row has empty `r2_key`.

## Definition of Done

- `pnpm db:migrate:plan:prod` succeeds.
- `pnpm content:sync:prod` succeeds without D1 conflict errors.
- Remote parity lane succeeds in authenticated environment.
- D1 remains sole lookup source; R2 manifests remain removed.

Status: complete as of 2026-04-06.
