# UI Revamp Completion Check and Priority Plan

## Goal

Confirm whether the UI revamp is done, then propose concrete near-term priorities with Search and Contribute first, Contact/About follow-up second, and other pipeline concerns listed by urgency.

## Assessment Result

Core UI revamp is effectively complete at the shell/component level, but not complete at the user-journey level.

- Revamp shell work appears shipped:
  - Global header/navigation and utility cluster are in place: `src/components/GlobalNavigation.astro`
  - Story-first homepage with support sections is in place: `src/pages/index.astro`
  - Collection hero + compact filter bar + unified discovery grid are in place: `src/components/CollectionHeroShell.astro`, `src/components/site/CollectionFilterBar.astro`, `src/components/DiscoveryCollectionView.astro`
  - Article context/header shell is in place: `src/components/site/ArticleContextHeader.astro`
  - Compact footer with About/Contribute/Contact/GitHub links is in place: `src/components/site/CompactFooter.astro`
  - Daisy-style class usage appears removed from current Astro markup/CSS surface (no `btn`, `badge`, `tabs`, or `daisyui` plugin usage detected in `src/`)

- Remaining blockers before calling it fully "finished":
  - Header Search is still a non-functional placeholder button (`aria-label="Global search coming soon"`): `src/components/GlobalNavigation.astro`
  - Contribute destination is still placeholder copy, no form flow yet: `src/pages/contribute.astro`
  - Contact page is still placeholder copy: `src/pages/contact.astro`
  - Maps is still explicit MVP placeholder: `src/pages/references/maps/index.astro`

## Important Note on About Contact Section

A Contact section already exists on About (`Contact and Transparency` plus email text): `src/pages/about.astro`.

Implication:
- The requested About contact presence is partially satisfied already.
- What is missing is stronger integration with the upcoming Contribute/Contact form journey (clear CTA/link consistency and shared messaging).

## Recommended Priorities

### P0 (Do next; highest urgency)

1. Implement global Search UX end-to-end
   - Add a real destination (`/search`) or modal flow from header Search.
   - Wire to existing backend search endpoint: `src/pages/api/search.ts`.
   - Include result states: empty, loading, unavailable, and auth-aware visibility messaging.

2. Implement Contribute form end-to-end
   - Convert `src/pages/contribute.astro` from placeholder to real form.
   - Reuse/extend current contact relay endpoint (`src/pages/api/contact.ts`) or create dedicated contribute endpoint if workflow differs.
   - Add anti-spam controls (honeypot exists at API level already), validation, success/error UX, and noindex posture for API responses.

### P1 (Immediately after P0)

3. Make Contact/About coherent with Contribute flow
   - Keep About as trust/transparency narrative.
   - Add explicit link path consistency between About -> Contact and Contribute -> Contact.
   - Replace `src/pages/contact.astro` placeholder with either concise contact options or shared form shell.

4. Resolve high-visibility placeholders still in navigation paths
   - Maps route content from placeholder to first usable curated surface: `src/pages/references/maps/index.astro`.
   - Review any "coming soon" copy on prominent routes and decide implement/hide/label policy.

### P2 (Pipeline but not blocking Search/Contribute)

5. Campaign auth model simplification program (Option 3 unified roles)
   - Status is accepted/planned, not complete: `plans/option-3-unified-membership-role-upgrade-todo.md`.
   - Important before adding more campaign auth complexity.

6. Campaign ID parsing hardening
   - Open medium-priority follow-up after prior production incident: `plans/todos/campaign-id-parsing-hardening-2026-04-10.md`.

7. Search evolution beyond baseline
   - Existing foundation endpoint is present; next phase is richer UX and eventual FTS expansion per discovery/search plans.

## Stepwise Execution Protocol (User-Requested)

Execution will proceed one gated step at a time with explicit approval checkpoints:

1. Implement exactly one step (starting with P0-1 only).
2. Run required verification gates:
   - `pnpm test`
   - `pnpm build`
3. Report what changed, test outcomes, and any known issues.
4. Stop and wait for explicit approval before starting the next step.

No dependency additions are planned for P0-1/P0-2.

## Immediate Implementation Scope: P0-1 Only

### P0-1 Goal

Ship a functional global Search UX (header entry point + search destination + result states) backed by the existing API: `src/pages/api/search.ts`.

### P0-1 Deliverables

1. Create a dedicated search page surface (recommended route: `/search`) with:
   - query input
   - results list
   - empty state
   - unavailable/error state
2. Wire the header Search control to the real destination instead of placeholder behavior in `src/components/GlobalNavigation.astro`.
3. Use the existing search backend contract (`/api/search`) for query execution.
4. Ensure messaging reflects visibility-aware results (public-only unless user context allows more, per current API behavior).

### P0-1 Acceptance Criteria

1. Clicking Search in global navigation opens a functional search experience.
2. Querying with at least 2 characters returns real results from `/api/search`.
3. Empty, invalid-query, and unavailable states are clearly handled.
4. No regressions in existing route rendering or navigation behavior.

### P0-1 Verification Gate (must pass before approval request)

1. `pnpm test`
2. `pnpm build`

### P0-1 Approval Checkpoint

After P0-1 implementation + passing verification gate, pause and request approval before P0-2.

## Next Step Queue (Do Not Start Yet)

- P0-2: Contribute form implementation.
- P1-3/P1-4: Contact/About coherence and Maps placeholder replacement, pending P0 completion and re-prioritization.

## Status Audit Update (2026-04-17)

User requested validation of P2-5 first, then P2-6.

### P2-5 (Campaign auth model simplification / Option 3 unified roles)

Assessment: **functionally implemented in code and migrations; docs/todo status is stale in one file.**

Evidence from repository:

1. Unified role migration exists with enforced role constraint:
   - `migrations/0009_campaign_memberships_role_unification.sql`
   - includes `CHECK (role IN ('member', 'gm'))`
2. Legacy GM table decommission migration exists:
   - `migrations/0010_drop_campaign_gm_assignments.sql`
3. Migration planner includes both 0009 and 0010 in active ordered plan:
   - `scripts/db-migrate-auth-plan.mjs`
4. Runtime membership/GM checks read `campaign_memberships` (role-based) rather than legacy GM table:
   - `src/lib/campaign-membership-repo.ts`
5. Runbook explicitly states D1 `campaign_memberships` role is canonical GM authority:
   - `docs/runbook/phase-2-1-auth-google-d1-mailjet-email.md`

Open caveat:

- `plans/option-3-unified-membership-role-upgrade-todo.md` still has unchecked items that appear already satisfied by code/migrations. This looks like planning-document drift, not a missing implementation.

### P2-6 (Campaign ID parsing hardening)

Assessment: **partially done, not fully closed**.

What is done:

1. Shared parsing helpers exist:
   - `src/utils/campaign-collections.ts`
   - `extractCampaignSlugFromEntryId`, `extractCampaignFamilySlugFromEntryId`, `extractLeafSlugFromEntryId`
2. Core campaign routes now use `extractCampaignSlugFromEntryId` for campaign slug matching.

What remains (still open against acceptance intent):

1. Route-local `id.split('/')` parsing still exists in campaign session routes:
   - `src/pages/campaigns/[campaign]/sessions/[...slug].astro`
   - `src/pages/campaigns/[campaign]/sessions/index.astro`
2. Sitemap campaign URL assembly still relies on hardcoded segment slicing:
   - `src/pages/sitemap.xml.ts`
3. Parser test coverage is incomplete vs requested shapes:
   - current tests only cover `{slug}/index` and legacy `campaigns/{slug}/...` in `src/utils/campaign-collections.test.ts`
   - missing explicit tests for `{slug}/sessions/{sessionSlug}` and `{slug}/{family}/{entrySlug}`

## Re-Prioritized Execution Recommendation

1. Mark P2-5 as complete in planning docs (documentation hygiene only).
2. Execute P2-6 as the next implementation step.

### P2-6 Implementation Slice (single step)

1. Replace remaining campaign route-local `id.split('/')` parsing with shared helpers from `src/utils/campaign-collections.ts`.
2. Replace sitemap hardcoded campaign/family slug slicing with helper-driven parsing.
3. Expand tests in `src/utils/campaign-collections.test.ts` to cover:
   - `campaigns/{slug}/...`
   - `{slug}/index`
   - `{slug}/sessions/{sessionSlug}`
   - `{slug}/{family}/{entrySlug}`
4. Verification gate:
   - `pnpm test`
   - `pnpm build`

## Proposed Execution Order (Short Program)

1. Search implementation (P0-1)
2. Contribute form implementation (P0-2)
3. Contact/About flow polish (P1-3)
4. Maps first useful surface (P1-4)
5. Campaign auth/data hardening items (P2-5, P2-6)

## Completion Criteria to Close "UI Revamp Finished"

Treat revamp as truly complete when all of the below are true:

1. Header Search opens a functional search experience and returns real results.
2. Contribute is a real submitted workflow (not placeholder text).
3. Contact page is no longer placeholder and aligns with About contact messaging.
4. No high-prominence placeholder routes remain in primary nav/support routes.
